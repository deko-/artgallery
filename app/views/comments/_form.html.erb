<%= form_for([@artwork, @artwork.comments.build]) do |f| %>
  <% if @artwork.comments.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork.comments.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @artwork.comments.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
